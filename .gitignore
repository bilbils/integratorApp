# dependencies / build output
node_modules/
dist/

# env / secrets
.env
*.local

# logs / OS cruft
*.log
.DS_Store
Thumbs.db

# secrets & local backups — never commit
.env.broken-backup
*.broken-backup
_to_delete/
